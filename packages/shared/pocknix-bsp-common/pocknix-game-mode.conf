# /run/pocknix holds the per-game performance override written by pocknix-proton-wrapper
# (runs as deck); pocknix-fancontrol and pocknix-lavd-mode read it. tmpfs, so a reboot clears it.
d /run/pocknix 0775 root deck -
