# Disable KSplash: plasmashell on Plasma Mobile never sends the "desktop ready" D-Bus signal,
# so the splash always runs its full 30s fallback timeout, covering an already-live shell and
# making every session switch look hung. System-wide default in /etc/xdg.
[KSplash]
Theme=None
Engine=none
