# Every image ships the same public root password, so root over SSH stays off whatever
# the Pocknix Tools toggle does. This beats sshd_config's own PermitRootLogin: that file
# includes this directory on line two and sshd keeps the first value it obtains.
PermitRootLogin no
