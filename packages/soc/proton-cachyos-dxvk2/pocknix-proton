#!/bin/sh
# pocknix-proton — compat-tool entrypoint shim; see pocknix-steam/pocknix-proton. Ships
# in-package because the tool dir is root-owned, out of reach of the user-level prep.
exec /usr/lib/pocknix/pocknix-proton-wrapper "$(dirname "$0")/proton" "$@"
