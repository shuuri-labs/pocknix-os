# pocknix-os build configuration
#
# Central settings sourced by every script in scripts/. Override any value by
# exporting it in your shell before invoking make, e.g.:
#
#   export DISTRIBUTION_DIR=/path/to/distribution
#   make build
#
# This file must stay POSIX-sh compatible (it is sourced, not executed).

# ---------------------------------------------------------------------------
# Target device
# ---------------------------------------------------------------------------
: "${DEVICE:=RP6}"            # Retroid Pocket 6
: "${SOC:=SM8550}"           # Qualcomm SM8550 / QCS8550

# Root partition label (kept compatible with the existing ROCKNIX install).
# Defined early because the kernel cmdline below references it.
: "${ROOT_LABEL:=POCKNIX_ROOT}"

# ---------------------------------------------------------------------------
# Layout (absolute paths derived from POCKNIX_ROOT, set by lib.sh)
# ---------------------------------------------------------------------------
: "${BUILD_DIR:=${POCKNIX_ROOT}/build}"
: "${ROOTFS_DIR:=${BUILD_DIR}/rootfs}"
: "${IMAGE_DIR:=${BUILD_DIR}/image}"
: "${VENDOR_DIR:=${POCKNIX_ROOT}/vendor}"
: "${KERNEL_DIR:=${POCKNIX_ROOT}/kernel}"
: "${CONFIG_DIR:=${POCKNIX_ROOT}/config}"
: "${PACKAGES_DIR:=${POCKNIX_ROOT}/packages}"
: "${CACHE_DIR:=${BUILD_DIR}/cache}"

# ---------------------------------------------------------------------------
# Arch Linux ARM base rootfs
# ALARM publishes a rolling "latest" tarball. For a hermetic/reproducible build
# set POCKNIX_ALARM_SHA256 to the digest of the tarball you intend to pin.
# Leave it empty to build against whatever "latest" currently is (a warning is
# printed and verification is skipped).
# ---------------------------------------------------------------------------
: "${ALARM_MIRROR:=http://os.archlinuxarm.org/os}"
: "${ALARM_TARBALL:=ArchLinuxARM-aarch64-latest.tar.gz}"
: "${POCKNIX_ALARM_SHA256:=}"

# ---------------------------------------------------------------------------
# Kernel source (Phase 1). Stock kernel.org, pinned — same base ROCKNIX uses
# for SM8550 (packages/linux/package.mk: PKG_VERSION/PKG_URL). The full kernel
# is this stock source + the patch stack under kernel/patches/ (applied in
# numeric subdir order: 10-mainline -> 20-sm8550 -> 30-version) + the config.
# Set KERNEL_SOURCE_SHA256 for reproducible builds.
# ---------------------------------------------------------------------------
: "${KERNEL_VERSION:=7.0.11}"
: "${KERNEL_SOURCE_URL:=https://www.kernel.org/pub/linux/kernel/v${KERNEL_VERSION%%.*}.x/linux-${KERNEL_VERSION}.tar.xz}"
: "${KERNEL_SOURCE_SHA256:=e56c8356dda01136a6041c6ef832bd0ec99bd2d35dff97832aa5ec10ed014304}"

# mkbootimg (Google) — builds the qcom-abl Android boot image. Pinned to the
# commit ROCKNIX uses (packages/tools/mkbootimg).
: "${MKBOOTIMG_URL:=https://android.googlesource.com/platform/system/tools/mkbootimg}"
: "${MKBOOTIMG_COMMIT:=d2bb0af5ba6d3198a3e99529c97eda1be0b5a093}"

# Kernel command line baked into the boot image. We boot a plain ext4 root with
# NO initramfs (UFS/SCSI/ext4 are built-in =y), so this uses a standard root=
# spec instead of ROCKNIX's LibreELEC 'boot=LABEL=/disk=LABEL='. The real SM8550
# params are ROCKNIX's EXTRA_CMDLINE for this SoC. ROOT spec is finalized at
# install time (Phase 6) once the partition layout is known; PARTLABEL needs a
# GPT partition named ${ROOT_LABEL}.
: "${KERNEL_CMDLINE_ROOT:=root=PARTLABEL=${ROOT_LABEL} rw}"
: "${KERNEL_CMDLINE_EXTRA:=quiet rootwait console=tty0 allow_mismatched_32bit_el0 fw_devlink.strict=1 pcie_ports=compat irqaffinity=0-2 cgroup.memory=nokmem,nosocket nosoftlockup usbcore.interrupt_interval_override=045e:028e:2 ufshcd_core.uic_cmd_timeout=3000 mem_sleep_default=deep}"
: "${KERNEL_CMDLINE:=${KERNEL_CMDLINE_ROOT} ${KERNEL_CMDLINE_EXTRA}}"

# Parallelism for the kernel compile
: "${JOBS:=}"

# ---------------------------------------------------------------------------
# SD boot-test image (build-sd-image.sh). Layout mirrors ROCKNIX's qcom-abl SD
# so the existing RP6 ABL boots it: GPT p1 fat32 partition-name "system" + FAT
# label SD_FAT_LABEL (KERNEL), p2 ext4 partition-name ${ROOT_LABEL} (rootfs).
# ---------------------------------------------------------------------------
: "${SD_FAT_LABEL:=ROCKNIX}"        # what the ABL looks for; = ROCKNIX's DISTRO_BOOTLABEL
: "${SD_BOOT_PARTNAME:=system}"     # GPT name of the FAT boot partition (ROCKNIX uses "system")
: "${SD_BOOT_MIB:=256}"             # size of the FAT boot partition
: "${SD_SLACK_MIB:=1024}"           # free space added to the root partition beyond the rootfs
: "${SD_ROOT_PASSWORD:=pocknix}"    # root password baked into the test image (CHANGE for real use)
# Optional Wi-Fi pre-seed: if set, the image auto-joins this network on boot so you
# can SSH over Wi-Fi (no USB cable). Pass at build time, e.g.:
#   sudo SD_WIFI_SSID='MyNet' SD_WIFI_PSK='secret' make sd-image
: "${SD_WIFI_SSID:=}"
: "${SD_WIFI_PSK:=}"
# Wi-Fi regulatory country (2-letter ISO code, e.g. GB/US/DE). REQUIRED for 5 GHz:
# regulatory.db only provides the rules; the domain must be SET or the kernel stays on
# the restrictive world domain (5 GHz channels become no-IR -> can't associate).
: "${SD_WIFI_COUNTRY:=}"

# ---------------------------------------------------------------------------
# Source of the ROCKNIX SM8550 kernel + device integration to vendor.
# Defaults to the user's local distribution checkout; override DISTRIBUTION_DIR
# (or ROCKNIX_GIT_URL for a git-based sync) as needed.
# ---------------------------------------------------------------------------
: "${DISTRIBUTION_DIR:=${POCKNIX_ROOT}/../distribution}"
: "${ROCKNIX_PROJECT_DIR:=${DISTRIBUTION_DIR}/projects/ROCKNIX}"
: "${ROCKNIX_DEVICE_DIR:=${ROCKNIX_PROJECT_DIR}/devices/${SOC}}"

# ---------------------------------------------------------------------------
# Cross-build / chroot helpers
# When building on an x86_64 host, package installation runs the aarch64 pacman
# under qemu-user-static via binfmt_misc.
# ---------------------------------------------------------------------------
: "${QEMU_AARCH64_STATIC:=/usr/bin/qemu-aarch64-static}"
